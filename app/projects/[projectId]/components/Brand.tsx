import React from 'react'

export default function Brand() {
  return (
    <div className='w-full text-orange-600 font-semibold'>Brand
    
    
        {/* <Reorder.Group values={itemArray} onReorder={setItemArray}>
    {itemArray.map((item, index) => {
      return (
        <Reorder.Item value={item} key={item}>
        <div key={index} className='mt-6 h-8 flex justify-center items-center bg-purple-200 w-full border border-black rounded-md'> mai {item} hn {index} </div>
        </Reorder.Item>
      )
    })}
    
    </Reorder.Group> */}
    
    
    
    </div>



    
  )
}
